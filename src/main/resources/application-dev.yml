server:
  port: 8400
spring:
  application:
    name: notification-service
  config:
    activate:
      on-profile: dev
    import: optional:configserver:http://config-service:8888
management:
  endpoints:
    web:
      exposure:
        include:
          - "refresh"
          - "bus-refresh"